"""Operator UI for the Pseudo-Domo MCP — a FastAPI app + static frontend that
wraps the same core functions the MCP tools expose, adding the interactive
browse → Analyze → Draft → Create workflow with catalog/schema config."""
